// Configuración del cliente.
// En producción (Vercel) este archivo se REGENERA automáticamente en el build
// a partir de la variable de entorno API_URL (ver build-config.mjs).
// En local, si queda vacío, api.js usa el fallback a Laragon.
window.ENV_API_URL = window.ENV_API_URL || "";
