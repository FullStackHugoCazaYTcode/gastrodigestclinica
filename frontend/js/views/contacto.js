import { mountFull } from "../ui.js";

// Stub temporal — se reemplaza en la Tarea 8.
export function renderContacto() {
  mountFull(`<section class="section"><div class="container"><h1>Contacto</h1><p class="text-muted">En construcción.</p></div></section>`);
}
