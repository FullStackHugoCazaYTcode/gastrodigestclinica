import { mountFull } from "../ui.js";

// Stub temporal — se reemplaza con el landing real en la Tarea 4.
export function renderHome() {
  mountFull(`<section class="section"><div class="container"><h1>Inicio</h1><p class="text-muted">Landing en construcción.</p></div></section>`);
}
