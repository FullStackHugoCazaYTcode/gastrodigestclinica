import { mountFull } from "../ui.js";

// Stub temporal — se reemplaza en la Tarea 7 (integración /api/medicos).
export function renderMedicos() {
  mountFull(`<section class="section"><div class="container"><h1>Médicos</h1><p class="text-muted">En construcción.</p></div></section>`);
}
