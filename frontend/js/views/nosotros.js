import { mountFull } from "../ui.js";

// Stub temporal — se reemplaza en la Tarea 6.
export function renderNosotros() {
  mountFull(`<section class="section"><div class="container"><h1>Nosotros</h1><p class="text-muted">En construcción.</p></div></section>`);
}
