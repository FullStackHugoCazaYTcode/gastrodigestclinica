import { mountFull } from "../ui.js";

// Stub temporal — se reemplaza en la Tarea 5 (bento + FAQ).
export function renderServicios() {
  mountFull(`<section class="section"><div class="container"><h1>Servicios</h1><p class="text-muted">En construcción.</p></div></section>`);
}
